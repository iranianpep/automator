sudo: required

language: python

python:
  - "2.7"
  - "3.3"

install:
  - sudo apt-get install -y python-logilab-common
  - pip install -U setuptools
  - pip install pytest pytest-cov
  - pip install codecov

script:
  - pytest

after_success:
  - codecov